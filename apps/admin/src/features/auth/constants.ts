export const AUTH_COOKIE_NAMES = {
  accessToken: 'ep_access_token',
  refreshToken: 'ep_refresh_token',
} as const;

export const AUTH_ROLES = {
  admin: 'ADMIN',
  customer: 'CUSTOMER',
} as const;
